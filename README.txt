This build scenario assumes that the user is operating on a linux machine.

Before running the program, ensure that you have run the following at least once:

./dependencies.sh

To run the program, run the following command:

./run.sh

You will find test commands in the file "Test Commands.txt"
